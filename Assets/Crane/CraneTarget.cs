using UnityEngine;
using System.Collections;

public class CraneTargetAlpha : MonoBehaviour
{
	/*
		private float x = transform.position.x;
	private float y = transform.position.y;
	private float z = transform.position.z;

	void Start()
	{
	x = transform.position.x;
	y = transform.position.y;
	z = transform.position.z;
	}


	void On ()
	{
		//renderer.material.color.a = alphaOn;
		transform.position = new Vector3(x, y, z);
	}

	void Off ()
	{
		transform.position = new Vector3(x, -10, z);
	}
*/

}
